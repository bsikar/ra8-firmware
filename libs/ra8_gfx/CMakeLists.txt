# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Brighton Sikarskie
#
# libs/ra8_gfx -- software 2D graphics primitives + bitmap font rendering
# layered on top of a caller-owned framebuffer (GLCDC + DRW ready).
#
# Picked up automatically by tests/CMakeLists.txt globbing. Board-side
# firmware examples that need it can add the sources directly until a
# CMake target is wired up here.
#
#

# Intentionally empty -- consumers reach into inc/ + src/ directly.
