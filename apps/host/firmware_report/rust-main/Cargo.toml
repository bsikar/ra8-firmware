[package]
name = "firmware_report"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false
build = "build.rs"

[dependencies]
firmware_report_provider = { path = "../rust" }

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
pedantic = "deny"
