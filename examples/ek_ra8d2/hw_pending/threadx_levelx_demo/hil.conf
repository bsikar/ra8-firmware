# Demoted from hw_validated/hil/ to hw_pending/ on 2026-05-18 because
# the firmware halts in demo_panic_halt during demo_setup_or_halt(),
# specifically at the second init step (ra_cgc_get_clock_hz). The
# tightened alive-mode check (symbol-name resolution against the .elf)
# now correctly flags it -- previously it passed silently. Same
# Octo-SPI bring-up area as threadx_filex_levelx_demo (which already
# stays in hw_validated/hil/ with a tight uart_scrape negative regex
# correctly catching its lx_nor_flash_format failure). Promote back
# once the panic_halt root cause is fixed.
HIL_MODE=alive
HIL_BOOT_S=2
