# Demoted from hw_validated/hil/ to hw_pending/ on 2026-05-18 because the
# firmware halts in demo_panic_halt during demo_setup_or_halt(); the
# tightened alive-mode check (PC symbol-name resolution against the .elf)
# now correctly flags it -- previously it passed silently.
# Local HIL 2026-06-10: the captured UART shows the real cause is
# "[lx] format failed" (NOT the clock-init / ra_cgc_get_clock_hz step
# earlier suspected) -- the same LevelX NOR format failure as
# threadx_filex_levelx_demo. Raw Octo-SPI works (flash_journal passes
# live, #44). Fix the LevelX driver (port/levelx/lx_nor_driver_ra_xspi.c),
# then both promote together.
HIL_MODE=alive
HIL_BOOT_S=2
