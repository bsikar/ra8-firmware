# HIL gate for usb_selftest_ospi_rw (USB self-loop: J7 HS <-> J11 FS cabled together, one image
# runs both roles). Pass banner on SCI8 / J-Link OB CDC at 115200.
HIL_MODE=uart_scrape
HIL_EXPECT="USB SELFTEST WRITABLE-OSPI PASS"
HIL_EXPECT_NEGATIVE="HardFault|hw_init_failed|mismatch at|TIMEOUT|FAILED"
HIL_TIMEOUT_S=90
