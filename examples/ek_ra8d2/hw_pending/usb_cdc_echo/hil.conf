# Demoted from hw_validated/hil/ to hw_pending/ on 2026-05-18 -- same
# pattern as commit 1f46ad3b (threadx_usbx_cdc_demo, usb_hid_device,
# usb_msc_device): the firmware halts in ra_exception_halt_loop during
# init. The tightened alive-mode check (symbol-name resolution against
# the .elf) now correctly flags it. Phase 4 root-causes the halt and
# validates via the Pi-as-USB-host hil_usb_cdc_echo path.
HIL_MODE=alive
HIL_BOOT_S=2
