# Demoted from hw_validated/hil/ to hw_pending/ on 2026-05-18: this
# app uses the FileX <-> SDHI driver shim (fx_media_driver_ra_sdhi.h)
# and depends on a Digilent PMOD MicroSD card being seated in Pmod2.
# Per the current project scope MicroSD is out of scope until the
# physical card and the SD-host bring-up are ready -- the closely-
# related tz_secure_only_sd was demoted in commit f0a9866c for the
# same reason. Promote back to hw_validated/hil/ once the SD card is
# wired and the FileX-on-SDHI bring-up is validated; the tightened
# alive check would otherwise correctly fail this app today.
HIL_MODE=alive
HIL_BOOT_S=2
