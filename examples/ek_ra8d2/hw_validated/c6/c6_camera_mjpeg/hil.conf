# Dedicated verifier builds a credential-free image, provisions Wi-Fi over UART
# after the READY prompt, proves health and audio, decodes two changing stills,
# and parses two complete changing frames from the multipart MJPEG endpoint.
HIL_MODE=c6_camera_livestream
HIL_SELF_BUILD=1
HIL_FRAME_WIDTH=640
HIL_FRAME_HEIGHT=480
