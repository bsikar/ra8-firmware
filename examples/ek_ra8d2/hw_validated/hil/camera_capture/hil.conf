# PASS is emitted only after OV5640 identity/configuration, a complete plausible
# 640x480 CEU frame and all four firmware-side RGB888 rotations succeed.
HIL_MODE=uart_scrape
HIL_EXPECT="verdict=PASS"
HIL_EXPECT_NEGATIVE="verdict=FAIL|HardFault"
HIL_TIMEOUT_S=30
