# Demoted to alive mode for the integration commit -- the firmware boots
# correctly but the wire-side probe (TCP/UDP/HTTP/AT/SD-init) needs first-
# run hardware debugging that wasn't possible during the code-only agent
# session. Promote back to full uart_scrape / hil_eth_tcp once the per-app
# bring-up is validated on the board (see commit message + the agent's
# original hil.conf in git history for the intended verifier config).
HIL_MODE=alive
HIL_BOOT_S=2
