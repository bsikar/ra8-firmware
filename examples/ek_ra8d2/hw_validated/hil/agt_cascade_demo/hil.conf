HIL_MODE=uart_scrape
# agt_cascade_demo emits "agt_cas: tick=XXXXXXXX\r\n" per AGT1 underflow.
# Match the literal banner prefix -- any non-zero tick counts as a pass.
HIL_EXPECT="agt_cas: tick="
HIL_EXPECT_NEGATIVE="hw_init_failed|agt.*failed|cgc_init failed|HardFault"
HIL_TIMEOUT_S=15
