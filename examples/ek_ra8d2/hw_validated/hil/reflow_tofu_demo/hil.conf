# On-silicon / emulator headless verification for missing-glyph tofu fallback (#686/#687).
# Lays out and renders a chapter with missing Kanji, Greek, Cyrillic, and math glyphs.
HIL_MODE=uart_scrape
HIL_EXPECT="reflow-tofu-demo: pages=1 PASS"
HIL_EXPECT_NEGATIVE="FAIL|HardFault|TIMEOUT"
HIL_TIMEOUT_S=20
