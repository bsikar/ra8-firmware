# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Brighton Sikarskie
#
# Machine-readable exclusion marker for examples/_unsupported/threadx_sdcard_demo (#401).
# The prose in this app's README says what it does; this file says, in a form a
# gate can read, WHY it is excluded from hardware validation. Keys and the
# `reason` taxonomy are enforced by scripts/checks/check_unsupported_exclusions.py.

app = "threadx_sdcard_demo"
reason = "removable-media"
also = []
needs_extra_hardware = false
evidence = "examples/_unsupported/threadx_sdcard_demo/src/main.c"
detail = """
Needs a formatted SD card seated in the board's socket before the run: the app calls ra8_sdcard_read_blocks(0, ...) every five seconds and reports a read failure with no card present. Nothing in CI can insert one.
"""
