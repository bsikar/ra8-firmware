# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Brighton Sikarskie
#
# Machine-readable exclusion marker for examples/_unsupported/threadx_https_client (#401).
# The prose in this app's README says what it does; this file says, in a form a
# gate can read, WHY it is excluded from hardware validation. Keys and the
# `reason` taxonomy are enforced by scripts/checks/check_unsupported_exclusions.py.

app = "threadx_https_client"
reason = "host-side-rig"
also = []
needs_extra_hardware = false
evidence = "examples/_unsupported/threadx_https_client/README.md"
detail = """
Needs a route to the public internet off the board's RMII connector, which means bridging or NAT-ing a workstation interface onto the board's subnet. The pinned leaf-certificate digest also ships as 32 zero bytes, so an uncustomised flash refuses to send the request by design.
"""
