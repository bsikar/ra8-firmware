# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Brighton Sikarskie
#
# Machine-readable exclusion marker for examples/_unsupported/usb_audio_device (#401).
# The prose in this app's README says what it does; this file says, in a form a
# gate can read, WHY it is excluded from hardware validation. Keys and the
# `reason` taxonomy are enforced by scripts/checks/check_unsupported_exclusions.py.

app = "usb_audio_device"
reason = "human-observation"
also = []
needs_extra_hardware = false
evidence = "examples/_unsupported/usb_audio_device/README.md"
detail = """
Enumerates as a USB Audio Class 1.0 device and feeds a 1 kHz sine into the isochronous IN endpoint; a pass means a host actually rendered the tone under the expected VID/PID, which no automated gate here observes.
"""
