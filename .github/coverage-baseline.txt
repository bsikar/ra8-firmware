# ra8-firmware host-test coverage baseline (gcovr Cobertura XML).
#
# Two whitespace-separated numbers, percentages with one decimal:
#
#   <statement_pct> <branch_pct>
#
# Initial values measured on 2026-05-02 against tests/build-cov/
# under the project's Linux devcontainer (Ubuntu 24.04, gcc 13.3,
# gcovr 7.0). Filter: libs/ra8_* + src/, excluding libs/third_party/
# and tests/.
#
# scripts/checks/check_coverage.py rejects any new build whose
# statement OR branch percentage drops below these numbers. Bump
# them upward only -- never relax the gate.
89.9 81.1
