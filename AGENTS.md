# Repository Working Guidance

Read [`CLAUDE.md`](CLAUDE.md) completely before changing this repository. It is
the single repository-wide source for coding rules, validation requirements,
hardware safety, and contribution workflow.

This file is intentionally a pointer instead of a second copy. Shared guidance
belongs in `CLAUDE.md`; keeping it in one place prevents tool-specific entry
points from drifting apart.

For planned Zig library work, follow the "Planned Zig library migration"
section in `CLAUDE.md` and [`docs/ZIG_MIGRATION.md`](docs/ZIG_MIGRATION.md).
