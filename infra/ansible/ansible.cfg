[defaults]
inventory = inventory/hosts.ini
roles_path = roles
host_key_checking = False
# Default stdout callback only -- the prettier `yaml` callback lives in the
# community.general collection, which the bootstrap does not install; requiring
# it here made every playbook run fail to load. Keep dependencies minimal.
