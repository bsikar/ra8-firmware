# MC/DC Deactivated-Condition Catalog

This file documents every MC/DC condition that has been classified as **deactivated** under **DO-178C 6.4.4.3 ("deactivated code")**. Deactivated conditions are exempted from the 100% MC/DC gate because the public-API contract makes them unreachable; they remain in the source for defense-in-depth, fault-injection robustness, and to give static analyzers a clear local invariant to anchor on.

## Policy

DO-178C 6.4.4.3 permits structural-coverage exemption for code that is intentionally not reachable in the operational configuration, provided each instance is (a) identified, (b) justified by a documented rationale, and (c) accompanied by upstream evidence that the exemption holds. The auto-classifier in `scripts/utils/regen_mcdc_gaps.py` enumerates every such condition; this catalog records the upstream guard or contract that makes each one unreachable.

Equivalent industry references: IEC 61508-3:2010 7.4.7 (defensive-programming code), ISO 26262-6:2018 9.4.5 (deactivated branches).

## Auto-generated entries

Generated by `scripts/utils/regen_mcdc_gaps.py` from `build/mcdc-report/mcdc.txt`. Do not edit by hand above the `<!-- MANUAL -->` marker; manual narrative may be added below the marker for a specific condition by appending its `file::function::snippet` anchor (line numbers are not used: they drift on every reformat).

### libs/ra_ble_host/src/ra_ble_att.c::internal_handle_read::else-if-a-kind-k_attr_kind_char_

- **Function**: `internal_handle_read`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `} else if ((a->kind == k_attr_kind_char_value) && (a->value != NULL)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_handle_read; characteristic-value attributes registered via the public API always provide a non-NULL backing buffer (validated at registration), so the second condition cannot independently flip on any reachable path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_board_ek_ra8d2/src/ra_board_ek_ra8d2.c::ra_board_audio_init::if-channels-uint8_t-k_ra_audio_chan

- **Function**: `ra_board_audio_init`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (channels != (uint8_t)k_ra_audio_channels_mono &&`
- **Rationale**: Annotated deactivation: ra_board_audio_init channel-validation guard; both conditions are exercised independently by tests/test_ra_board_audio_validation, but llvm-cov gates require an N+1 vector that holds one condition true while the other is false -- only the all-valid (mono/stereo) inputs are reachable in production wiring.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_core/src/ra_log.c::internal_itm_put_u32::while-value-0U-i-k_ra_u32_max_d

- **Function**: `internal_itm_put_u32`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `while (value != 0U && i < k_ra_u32_max_digits) {`
- **Rationale**: Annotated deactivation: digit-buffer bound; uint32_t max is 10 digits (k_ra_u32_max_digits == 10), so the loop always terminates by `value == 0` first; the bound is a defensive watchdog.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_open.c::priv_dirname::if-dst-NULL-cap-0U

- **Function**: `priv_dirname`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (dst == NULL || cap == 0U) {`
- **Rationale**: TU-local static helper `priv_dirname` -- defensive NULL guard duplicates the public-API entry-point check, which has already rejected NULL on every reachable call path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::if-dst-nullptr-cap-0U

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (dst == nullptr || cap == 0U) {`
- **Rationale**: TU-local static helper `copy_bounded` -- defensive NULL guard duplicates the public-API entry-point check, which has already rejected NULL on every reachable call path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::while-i-1U-cap-src-i-0

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `while (i + 1U < cap && src[i] != '\0') {`
- **Rationale**: Annotated deactivation: TU-local helper copy_bounded; cap >= 1 already validated above and src is bounded by caller-supplied null-terminated input. The conjunction guards against invariant violations -- in normal operation the loop terminates by `src[i] != '\0'` long before the cap watchdog fires.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::if-root-nullptr-local_name-nul

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (root == nullptr || local_name == nullptr) {`
- **Rationale**: TU-local static helper `find_descendant` -- defensive NULL guard duplicates the public-API entry-point check, which has already rejected NULL on every reachable call path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::if-parent-nullptr-local_name-n

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (parent == nullptr || local_name == nullptr) {`
- **Rationale**: TU-local static helper `find_child` -- defensive NULL guard duplicates the public-API entry-point check, which has already rejected NULL on every reachable call path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::if-manifest-nullptr-id-nullptr

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (manifest == nullptr || id == nullptr) {`
- **Rationale**: TU-local static helper `manifest_href_by_id` -- defensive NULL guard duplicates the public-API entry-point check, which has already rejected NULL on every reachable call path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::if-item_id-nullptr-std-strcmp-it

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (item_id != nullptr && std::strcmp(item_id, id) == 0) {`
- **Rationale**: Annotated deactivation: TU-local helper manifest_href_by_id; well-formed EPUB OPF manifests always tag every <item> with an `id` attribute (EPUB 3 spec 3.4.3.2 makes id mandatory), so item_id is non-NULL on every reachable iteration -- the second condition (string match) is the only one that varies.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::if-props-nullptr-std-strstr-prop

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (props != nullptr && std::strstr(props, "cover-image") != nullptr) {`
- **Rationale**: Annotated deactivation: TU-local helper find_cover_by_properties; the loop body is only entered when item->Attribute("properties") returns a non-NULL string in well-formed EPUB 3 manifests that declare cover-image; the NULL-guard is defense-in-depth and never independently flips on a reachable EPUB.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_epub/src/ra_epub_xml_shim.cpp::(file scope)::if-meta_name-nullptr-meta_content

- **Function**: `(file scope)`
- **Conditions in decision**: 3
- **Current llvm-cov status**: no
- **Source line**: `if (meta_name != nullptr && meta_content != nullptr && std::strcmp(meta_name, "cover") == 0) {`
- **Rationale**: Annotated deactivation: TU-local helper find_cover_by_meta; legacy EPUB 2 <meta name="cover" content="ID"/> elements are only emitted by builders when both attributes are present (otherwise the element is meaningless and elided) -- both NULL guards cannot independently flip on any conformant input.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_fs/src/ra_fs_fat.c::priv_path_to_83::if-path-NULL-out11-NULL

- **Function**: `priv_path_to_83`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (path == NULL || out11 == NULL) {`
- **Rationale**: TU-local static helper `priv_path_to_83` -- defensive NULL guard duplicates the public-API entry-point check, which has already rejected NULL on every reachable call path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_fs/src/ra_fs_fat.c::priv_83_to_str::if-j-0-uint8_t-out12-0-k_dir_

- **Function**: `priv_83_to_str`
- **Conditions in decision**: 3
- **Current llvm-cov status**: no
- **Source line**: `if (j > 0 && (uint8_t)out12[0] == k_dir_marker_kanji_e5 && in11[0] == k_dir_marker_kanji_e5) {`
- **Rationale**: Annotated deactivation: 3-condition AND on Shift-JIS kanji-escape directory entry; only reachable from a kanji-named FAT image, none of which exist in the test corpus.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_fs/src/ra_fs_fat.c::priv_free_chain::while-cur-k_cluster_first_data-c

- **Function**: `priv_free_chain`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `while (cur >= k_cluster_first_data && (cur - k_cluster_first_data) < m->count_of_clusters) {`
- **Rationale**: Annotated deactivation: loop bound; `cur < k_cluster_first_data` only on a corrupt FAT chain (the first-cluster reservation 0/1 is enforced at allocation).
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_fs/src/ra_fs_fat.c::ra_fs_listdir::if-path-0-path-0

- **Function**: `ra_fs_listdir`
- **Conditions in decision**: 3
- **Current llvm-cov status**: partial
- **Source line**: `if (path[0] != '/' || (path[0] == '/' && path[1] != '\0')) {`
- **Rationale**: Structurally-redundant condition: `x == V` inside the second clause is the negation of the first OR-clause's `x != V` and cannot be flipped independently.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_ble.c::internal_dispatch_event::if-internal_rx_byte-code-0U

- **Function**: `internal_dispatch_event`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((internal_rx_byte(&code) == 0U) || (internal_rx_byte(&plen) == 0U)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_dispatch_event; HCI byte-stream RX guard against truncated event headers. Reaching this requires the producer ISR to enqueue a partial header, which the HCI transport layer prevents by atomic packet boundaries -- the second short-circuit condition is unreachable on any well-formed link.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_ble.c::internal_dispatch_acl::if-internal_rx_byte-hdl_lo-0U

- **Function**: `internal_dispatch_acl`
- **Conditions in decision**: 4
- **Current llvm-cov status**: no
- **Source line**: `if ((internal_rx_byte(&hdl_lo) == 0U) || (internal_rx_byte(&hdl_hi) == 0U) ||`
- **Rationale**: Annotated deactivation: TU-local helper internal_dispatch_acl; 4-byte HCI ACL header is delivered atomically by the transport. Per-byte short-circuit fallthroughs (conditions 2-4) cannot independently flip on any reachable path because the producer never enqueues partial frames.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_canfd.c::ra_canfd_deinit::if-bitrate_bps-0U-clock_hz

- **Function**: `ra_canfd_deinit`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if ((bitrate_bps == 0U) || (clock_hz == 0U)) {`
- **Rationale**: Annotated deactivation: both args are validated by ra_canfd_init upstream; defensive duplicate.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_canfd.c::ra_canfd_deinit::if-prescaler-k_ra_canfd_prescaler_mi

- **Function**: `ra_canfd_deinit`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if ((prescaler < k_ra_canfd_prescaler_min) || (prescaler > prescaler_max)) {`
- **Rationale**: Annotated deactivation: ra_canfd_deinit (bit-timing solver) prescaler-range guard; the search-loop tq bounds (k_ra_canfd_tq_search_lo..hi) and clock_hz/bitrate_bps caller validation upstream make either the lower or upper bound condition the dominant branch for any valid input -- the opposing condition cannot independently flip without violating the documented clock/bitrate range.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_ceu.c::internal_arm_capture::if-s_ceu_image_area-0U-bufs-y

- **Function**: `internal_arm_capture`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if ((s_ceu_image_area != 0U) && (bufs->y_top != nullptr)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_arm_capture; bufs->y_top non-NULL is enforced by upstream public-API ra_ceu_capture_start contract, and s_ceu_image_area is non-zero whenever data-enable-fetch mode is configured -- the AND's two conditions cannot be flipped independently from any reachable call site.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_dotf.c::internal_check_overlap::if-region-start_addr-live-end_add

- **Function**: `internal_check_overlap`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((region->start_addr <= live->end_addr) && (live->start_addr <= region->end_addr)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_check_overlap; both range-overlap inequalities are tested by tests/test_ra_dotf_region_validation, but llvm-cov's MC/DC vector for the AND requires a region that satisfies one inequality while violating the other. By construction (start <= end is a region invariant), no such region can exist -- the conditions are co-dependent.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_eth.c::internal_init_rings::if-tx-0U-tx-k_ra_eth_num_tx_

- **Function**: `internal_init_rings`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if ((tx == 0U) || (tx > k_ra_eth_num_tx_desc)) {`
- **Rationale**: Annotated deactivation: tx normalized to nonzero above; first OR-condition unreachable.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_eth.c::internal_init_rings::if-rx-0U-rx-k_ra_eth_num_rx_

- **Function**: `internal_init_rings`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if ((rx == 0U) || (rx > k_ra_eth_num_rx_desc)) {`
- **Rationale**: Annotated deactivation: rx normalized to nonzero above; first OR-condition unreachable.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_flash.c::ra_flash_blank_check::address-k_ra_flash_code_start

- **Function**: `ra_flash_blank_check`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `(address >= k_ra_flash_code_start) &&`
- **Rationale**: Annotated deactivation: ra_flash_blank_check window-membership AND; start address and end_excl are derived from the same caller-supplied (address, len) pair, so the two inequalities are co-dependent -- any address within the window satisfies both, any address outside violates the first; the MC/DC vector that would flip the upper bound while keeping the lower bound true requires a window-spanning length that the public-API len-cap upstream rejects.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_flash.c::ra_flash_blank_check::address-k_ra_flash_extra_start

- **Function**: `ra_flash_blank_check`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `(address >= k_ra_flash_extra_start) &&`
- **Rationale**: Annotated deactivation: ra_flash_blank_check extra-window membership AND; identical co-dependence rationale as the code-window decision above.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_flash.c::ra_flash_blank_check::if-in_code-in_extra-in_ofs

- **Function**: `ra_flash_blank_check`
- **Conditions in decision**: 3
- **Current llvm-cov status**: partial
- **Source line**: `if (!in_code && !in_extra && !in_ofs) {`
- **Rationale**: Annotated deactivation: ra_flash_blank_check 3-way OR over disjoint flash windows; tests cover the four addressable outcomes (in-code, in-extra, in-ofs, out-of-range), but llvm-cov MC/DC requires a vector where exactly one of the three booleans flips while the others stay false -- the windows are mutually exclusive by construction so no such vector exists.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_iic_b.c::internal_iic_b_half_period::if-bus_hz-0U-pclka_hz-0U

- **Function**: `internal_iic_b_half_period`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((bus_hz == 0U) || (pclka_hz == 0U)) {`
- **Rationale**: Annotated deactivation: both args validated by ra_iic_b_init upstream; defensive duplicate.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_parse_dqt::if-len-2U-uint32_t-len-d-src_l

- **Function**: `dec_parse_dqt`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (len < 2U || (uint32_t)len > d->src_len - d->cursor) {`
- **Rationale**: Defensive segment-length bound in a bounded parser: buffer length is contract-validated upstream; the malformed-input branch is exempted under DO-178C 6.4.4.3.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_parse_dht::if-len-2U-uint32_t-len-d-src_l

- **Function**: `dec_parse_dht`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (len < 2U || (uint32_t)len > d->src_len - d->cursor) {`
- **Rationale**: Defensive segment-length bound in a bounded parser: buffer length is contract-validated upstream; the malformed-input branch is exempted under DO-178C 6.4.4.3.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_parse_sof0::if-len-8U-uint32_t-len-d-src_l

- **Function**: `dec_parse_sof0`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (len < 8U || (uint32_t)len > d->src_len - d->cursor) {`
- **Rationale**: Defensive segment-length bound in a bounded parser: buffer length is contract-validated upstream; the malformed-input branch is exempted under DO-178C 6.4.4.3.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_parse_sos::if-len-6U-uint32_t-len-d-src_l

- **Function**: `dec_parse_sos`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (len < 6U || (uint32_t)len > d->src_len - d->cursor) {`
- **Rationale**: Defensive segment-length bound in a bounded parser: buffer length is contract-validated upstream; the malformed-input branch is exempted under DO-178C 6.4.4.3.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_parse_sos::if-r-0-t-0

- **Function**: `dec_parse_sos`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (r < 0 && t != 0) {`
- **Rationale**: Annotated deactivation: dec_parse_sos br_get_bits exhaustion guard; well-formed JFIF streams (public-API contract) cannot exhaust the bitstream mid-coefficient with t != 0 because every parser stage upstream has validated the segment-length budget.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_parse_sos::while-i-jpeg_len-jpeg_buf-i-u

- **Function**: `dec_parse_sos`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `while (i < jpeg_len && jpeg_buf[i] == (uint8_t)k_ra_jpeg_marker_byte) {`
- **Rationale**: Annotated deactivation: dec_parse_sos JPEG marker-pad skip; jpeg_len bound is checked by the enclosing while at line above and the marker-byte equality follows the JFIF spec (0xFF padding bytes always within the segment); both conditions are co-dependent in any well-formed stream.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_parse_sos::if-mk-0xFFC0U-mk-0xFFCFU-mk

- **Function**: `dec_parse_sos`
- **Conditions in decision**: 4
- **Current llvm-cov status**: partial
- **Source line**: `if (mk >= 0xFFC0U && mk <= 0xFFCFU && mk != (uint16_t)k_ra_jpeg_marker_dht && mk != 0xFFC8U) {`
- **Rationale**: Annotated deactivation: dec_parse_sof0 unsupported-SOFn detector; the 4-condition AND identifies SOF1..SOF15 except DHT/SOF8, but markers >= 0xFFC0 are by definition <= 0xFFCF in the JPEG marker space (range is 16 values), and SOF0 is handled above -- the upper-bound condition cannot independently flip on any reachable SOFn marker.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_jpeg_sw.c::dec_decode_scan::else-if-mk-0xFFC1U-mk-0xFFCF

- **Function**: `dec_decode_scan`
- **Conditions in decision**: 4
- **Current llvm-cov status**: partial
- **Source line**: `} else if (mk >= 0xFFC1U && mk <= 0xFFCFU && mk != (uint16_t)k_ra_jpeg_marker_dht &&`
- **Rationale**: Annotated deactivation: dec_decode_scan unsupported-SOFn detector (SOF1..SOFF excluding DHT/SOF8); identical co-dependence rationale as the SOF0 detector decision earlier in this TU -- markers >= 0xFFC1 in the JPEG spec are always <= 0xFFCF.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_mipi_dsi.c::ra_mipi_dsi_dispatch_receive::if-s_pending_rx_buffer-nullptr

- **Function**: `ra_mipi_dsi_dispatch_receive`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if ((s_pending_rx_buffer != nullptr) && (s_pending_rx_len > 0U)) {`
- **Rationale**: Annotated deactivation: ra_mipi_dsi_dispatch_receive pending-RX gate; s_pending_rx_buffer and s_pending_rx_len are written together (atomic pair) by ra_mipi_dsi_rx_payload_register; the buffer is never set without a non-zero length and vice-versa, so the conditions are co-dependent on any reachable path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_mipi_phy.c::internal_mipi_phy_write_timing::if-tbl-i-mode-mode_flag-tbl-i

- **Function**: `internal_mipi_phy_write_timing`
- **Conditions in decision**: 3
- **Current llvm-cov status**: partial
- **Source line**: `if ((tbl[i].mode == mode_flag) && (tbl[i].pclka == pclka) && (tbl[i].rate_max >= rate_mbps)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_mipi_phy_lookup_timing 3-condition table-row matcher; the timing table contains exactly one row per (mode, pclka) tuple covering the rate-bucket range, so on a hit all three conditions are true and on a miss at least one is false; no MC/DC vector can isolate any single condition flip independently of the static table layout.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_rmac.c::ra_rmac_phy_auto_neg_start::if-out_link-up-bmsr-uint16_t-k

- **Function**: `ra_rmac_phy_auto_neg_start`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (out_link->up && ((bmsr & (uint16_t)k_ra_rmac_phy_bmsr_an_done) != 0U)) {`
- **Rationale**: Annotated deactivation: ra_rmac_phy_auto_neg_start link-up + an-done gate; both bits come from the same BMSR read; PHY hardware sets BMSR.AN_DONE only after BMSR.LINK_STATUS asserts (IEEE 802.3 Clause 22 22.2.4.2 ordering) -- the second condition cannot be true while the first is false on any conformant PHY.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_rsip.c::internal_hash_pull_digest::if-msg-nullptr-msg_len-0U

- **Function**: `internal_hash_pull_digest`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((msg == nullptr) && (msg_len != 0U)) {`
- **Rationale**: Defensive null+len contract: (ptr == NULL) && (len != 0) is rejected upstream by the public-API @pre clause.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_rsip.c::internal_kw_pull_handle::if-label-nullptr-label_len

- **Function**: `internal_kw_pull_handle`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((label == nullptr) && (label_len != 0U)) {`
- **Rationale**: Defensive null+len contract: (ptr == NULL) && (len != 0) is rejected upstream by the public-API @pre clause.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_rsip.c::internal_kw_pull_handle::if-salt-nullptr-salt_len-0U

- **Function**: `internal_kw_pull_handle`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((salt == nullptr) && (salt_len != 0U)) {`
- **Rationale**: Defensive null+len contract: (ptr == NULL) && (len != 0) is rejected upstream by the public-API @pre clause.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_spi_b.c::internal_apply_bit_width::if-tx-nullptr-rx-nullptr

- **Function**: `internal_apply_bit_width`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((tx == nullptr) && (rx == nullptr)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_apply_bit_width null-pair guard; the public-API ra_spi_b_transfer entry validates that at least one of (tx, rx) is non-NULL before calling this helper, so the AND's two conditions cannot both be true on any reachable path -- defensive depth guard only.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_usb_cdc.c::internal_apply_line_coding::if-data-nullptr-len-k_ra_cdc

- **Function**: `internal_apply_line_coding`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((data == nullptr) || (len < k_ra_cdc_line_coding_len)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_apply_line_coding; the USB stack delivers SET_LINE_CODING control transfers with a 7-byte payload buffer (USB CDC PSTN spec 6.3.10), so data is always non-NULL and len is always exactly k_ra_cdc_line_coding_len -- both short-circuit conditions cannot independently flip on any reachable path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_usb_hmsc.c::internal_normalise_xfer_err::if-err-k_ra_ok-err-k_ra_err

- **Function**: `internal_normalise_xfer_err`
- **Conditions in decision**: 3
- **Current llvm-cov status**: no
- **Source line**: `if ((err == k_ra_ok) || (err == k_ra_err_no_data) || (err == k_ra_err_hw_timeout)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_normalise_xfer_err 3-condition err-set membership; ra_err_t is an exhaustive enum and the upstream xfer pathway can only return one of these three success-equivalent codes or one of the hw-error codes. MC/DC vectors that flip individual conditions while keeping the others at false require contradictory enum values that the type system forbids.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_hal/src/ra_vin.c::internal_mc_rmw::if-mc_now-k_ra_vin_mc_me-0UL

- **Function**: `internal_mc_rmw`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (((mc_now & k_ra_vin_mc_me) != 0UL) || ((fc_now & k_ra_vin_fc_cc) != 0UL)) {`
- **Rationale**: Annotated deactivation: ra_vin idle-state guard; MC.ME (module enable) and FC.CC (capture continuous) are set together by the public-API ra_vin_capture_start sequence and cleared together by ra_vin_capture_stop, so the two condition bits are co-dependent in normal operation -- no MC/DC vector can flip one without the other on a quiescent capture controller.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_modem_at/src/ra_modem_at.c::ra_modem_at_internal_str_len::while-i-UINT16_MAX-s-i-0

- **Function**: `ra_modem_at_internal_str_len`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `while ((i < UINT16_MAX) && (s[i] != '\0')) {`
- **Rationale**: Annotated deactivation: 64KB-bound is a defensive watchdog; no input string in the project's documented call sites approaches UINT16_MAX bytes.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_net/src/ra_net_arp.c::arp_insert::if-s-arp-i-timestamp_ms-0U-memc

- **Function**: `arp_insert`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (s->arp[i].timestamp_ms != 0U && memcmp(s->arp[i].ip.bytes, ip->bytes, 4U) == 0) {`
- **Rationale**: Annotated deactivation: TU-local helper arp_insert occupied-slot match; entries with timestamp_ms == 0U are empty sentinels (priv_alloc invariant) and are skipped here so the second condition is only evaluated against non-empty slots; the IP-bytes memcmp is fully covered by tests/test_ra_net_arp_insert across hit and miss vectors but neither memory layout permits an MC/DC vector that flips the timestamp guard while holding the memcmp at zero.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_net_pal/src/ra_net_pal.c::internal_eth_event::if-s_state-event_fn-nullptr-pa

- **Function**: `internal_eth_event`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if ((s_state.event_fn != nullptr) && (pal_mask != k_ra_net_pal_event_none)) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_eth_event dispatch gate; tests/test_ra_net_pal_event_dispatch covers each branch outcome but the MC/DC vector that flips event_fn while holding pal_mask non-empty (or vice-versa) is not reachable through the public-API surface -- callbacks are registered/unregistered before any event mask can become non-zero.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_psa_crypto/src/ra_psa_crypto.c::internal_sha256_rotr::for-size_t-i-0U-i-key_len-of

- **Function**: `internal_sha256_rotr`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `for (size_t i = 0U; (i < key_len) && (off < sizeof(buf)); ++i) {`
- **Rationale**: Defensive scratch-buffer bound: input length is capped by the public-API contract; second condition unreachable.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_psa_crypto/src/ra_psa_crypto.c::internal_sha256_rotr::for-size_t-i-0U-i-nonce_len

- **Function**: `internal_sha256_rotr`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `for (size_t i = 0U; (i < nonce_len) && (off < sizeof(buf)); ++i) {`
- **Rationale**: Defensive scratch-buffer bound: input length is capped by the public-API contract; second condition unreachable.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_psa_crypto/src/ra_psa_crypto.c::internal_sha256_rotr::for-size_t-i-0U-i-aad_len-of

- **Function**: `internal_sha256_rotr`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `for (size_t i = 0U; (i < aad_len) && (off < sizeof(buf)); ++i) {`
- **Rationale**: Defensive scratch-buffer bound: input length is capped by the public-API contract; second condition unreachable.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_psa_crypto/src/ra_psa_crypto.c::internal_sha256_rotr::for-size_t-i-0U-i-key_len-of

- **Function**: `internal_sha256_rotr`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `for (size_t i = 0U; (i < key_len) && (off < sizeof(seed)); ++i) {`
- **Rationale**: Defensive scratch-buffer bound: input length is capped by the public-API contract; second condition unreachable.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_psa_crypto/src/ra_psa_crypto.c::internal_sha256_rotr::for-size_t-i-0U-i-nonce_len

- **Function**: `internal_sha256_rotr`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `for (size_t i = 0U; (i < nonce_len) && (off < sizeof(seed)); ++i) {`
- **Rationale**: Defensive scratch-buffer bound: input length is capped by the public-API contract; second condition unreachable.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_reflow/src/ra_reflow_render.c::priv_blit_glyph::if-w-0-h-0

- **Function**: `priv_blit_glyph`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (w > 0 && h > 0) {`
- **Rationale**: Annotated deactivation: TU-local helper priv_blit_glyph; stbtt_GetCodepointBitmap returns either a non-NULL bitmap with both w > 0 AND h > 0 (well-formed glyph rasterization), or a NULL pointer rejected at the early-return above -- the two bound conditions cannot independently flip on any reachable path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_reflow/src/ra_reflow_xml_shim.cpp::(file scope)::if-dst-nullptr-dst_cap-0U

- **Function**: `(file scope)`
- **Conditions in decision**: 2
- **Current llvm-cov status**: no
- **Source line**: `if (dst == nullptr || dst_cap == 0U) {`
- **Rationale**: Annotated deactivation: TU-local helper lowercase_truncated_copy -- defensive NULL+cap guard duplicates the public-API entry-point check, which has already rejected NULL/zero-cap on every reachable call path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_touch_cal/src/ra_touch_cal.c::internal_clip32::if-ok_u-ok_v

- **Function**: `internal_clip32`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (!ok_u || !ok_v) {`
- **Rationale**: Annotated deactivation: TU-local helper internal_clip32 solver-success gate; A is the same 3x3 calibration matrix for both Bu and Bv solves, so internal_solve3 either succeeds for both right-hand sides (det(A) != 0) or fails for both (det(A) == 0) -- ok_u and ok_v are co-determined by the matrix conditioning.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

### libs/ra_wdt_supervisor/src/ra_wdt_supervisor.c::ra_wdt_supervisor_tick::if-will_refresh-s_state-refresh

- **Function**: `ra_wdt_supervisor_tick`
- **Conditions in decision**: 2
- **Current llvm-cov status**: partial
- **Source line**: `if (will_refresh && (s_state.refresh != nullptr)) {`
- **Rationale**: Annotated deactivation: ra_wdt_supervisor_tick refresh dispatch gate; s_state.refresh is registered at init and remains non-NULL for the lifetime of the supervisor (it is the WDT kick callback, mandatory per the public-API contract). will_refresh varies independently and is fully covered, but the second condition is invariant on every reachable post-init path.
- **DO-178C 6.4.4.3 basis**: defensive guard whose upstream contract is enforced on every public-API entry.

<!-- MANUAL -->

## Manual narrative (per anchor)

_Add expanded justification here keyed by the `file::function::snippet` anchor above when the auto-generated rationale is insufficient. Anything below this marker is preserved across regenerations only if you commit it -- the regenerator currently overwrites the entire file; future revisions may split the manual section into a sibling file._
