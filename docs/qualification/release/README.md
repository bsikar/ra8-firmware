# Release qualification archives

This directory owns the planned `docs/qualification/release/<tag>/` layout.
There are no release archives yet. When a signed release is cut, its immutable
gate reports, hashes, and conformance record are stored in a directory named
for that release tag. The release procedure in `docs/qualification/SCMP.md`
defines when those directories may be created.
