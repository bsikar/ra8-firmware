# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Brighton Sikarskie
#
# Pinned generator set for the documentation hub (ADR-0005, #900).
#
# Pinned for the same reason the doxygen release is pinned: the published
# site must not change because a machine happened to resolve a newer
# renderer. scripts/builders/docs_hub.sh installs exactly this file into a
# venv under build/tools/ and builds from it.
#
# Bump deliberately, rebuild with `just docs::hub`, and look at the output.
mkdocs==1.6.1
mkdocs-material==9.6.22
